---
name: guanguan-director
description: 从待拍影视剧本或可拍场景文本推导统一导演命题、视听宪法，并完成节奏规划、视听化微调、镜头组、分镜拆解、分镜优化讨论和分镜表输出。用户明确需要导演分析、导演阐述、影片拍摄方向、剧本如何拍、分镜拆解，或希望讨论、修改已有镜头/镜头组/分镜表时使用。多类型项目先建立统一导演语法，再处理段落级变化。不用于文学文本视觉想象、单镜头或视频生成提示词、故事板图片生成、已拍素材分析、剪辑工程及纯美术或器材方案。
---

# 关关导演

从故事内部建立导演方法。不要从导演名单或类型标签出发，不要把多类型题材机械翻译成多种导演风格。

## 核心原则

1. 先判断剧本要求观众看见、知道和感受什么，再决定如何拍。
2. 区分故事主体、世界规则、信息机制、情绪基调和社会语境。多类型不等于多风格。
3. 先建立一套全片稳定的视听语法，再允许段落级变量和少量有理由的例外。
4. 把导演案例当作方法证据，不当作模仿目标。所有引用必须改写成可执行规则。
5. 任何局部方法若与全片视听宪法冲突，必须舍弃或明确修改宪法，不能含糊地称为“融合”。
6. 所有判断必须能追溯到剧本证据。缺乏证据时标注为“待确认假设”。

## 声明式工作流协议

进入 N1 前必须先读取 [00D-输入归档与版本协议.md](references/workflow/00D-输入归档与版本协议.md)，将用户提供的剧本、故事摘要或交接文本存入项目运行目录的 `input/`。后续节点必须从该归档或完整对话检查点读取源文本，不得仅依赖前文上下文记忆。

先读取 [00-声明式工作流协议.md](references/workflow/00-声明式工作流协议.md)、[00A-循环检查点与上下文控制.md](references/workflow/00A-循环检查点与上下文控制.md)、[00B-输入边界与交接协议.md](references/workflow/00B-输入边界与交接协议.md) 和 [00C-审核策略与自动通过规则.md](references/workflow/00C-审核策略与自动通过规则.md)，再执行下面六个业务节点。前者定义节点、回退和确认闸门，后者定义按循环单元写入、恢复和最小读取集，00B 定义触发、输入归一化和规模边界，00C 定义审核级别与自动通过条件；本节只定义每个节点要完成的工作。它们是给单个 Agent 使用的行为协议，不是独立的 Graph 运行时。

不得把六个节点当成一次性提示词连续生成。宿主具备文件能力时，每个循环单元都必须写入 `_work/` 检查点并更新 `00-state.yaml`；下一轮只加载当前单元的最小输入集。再通过有界节点内循环、自检和闸门继续。遇到无法完全解决的问题，不得无限等待或死循环；按编排规则降级并登记问题后继续。

## 当前工作流

完整新项目严格依次完成六个阶段。已有产物、检查点或局部任务按 00B 从最早有效节点进入。审核按 00C 分为 `auto`、`sample`、`human`：低风险节点自动继续，重点或异常内容抽样展示，只有创作方向、结构性变化和用户明确要求的内容才暂停等待确认。

### 第一步：剧本导演分析

读取并执行 [01-剧本导演分析.md](references/workflow/01-剧本导演分析.md)。

目标不是复述剧情，而是建立后续导演决策的因果依据：

- 识别故事主体、核心冲突、人物关系变化和价值转变。
- 区分类型成分在项目中的功能与主次。
- 梳理观众信息、角色信息和信息释放顺序。
- 提取核心空间、动作、物件、声音和可发展的意象。
- 标出不可拍内容、视听机会、制作约束和关键风险。
- 形成“导演问题清单”，暂不选择导演风格。

完整剧本且无高风险假设时，G1 自动通过并报告摘要；存在缺失、冲突或多个核心解释时暂停人工审核。

### 第二步：导演命题与全片视听宪法

读取并执行 [02-导演命题与视听宪法.md](references/workflow/02-导演命题与视听宪法.md)。

先从第一步结论提出 2-3 个彼此真正不同的导演命题方向。差异必须是摄影机立场、信息关系或情感表达机制的差异，不能只是色调和滤镜差异。

同一份 G2 输出同时展示候选方向、推荐方向及其全片视听宪法草案，避免先选方向、再确认宪法的两次暂停。用户一次确认后固化，至少覆盖：

- 摄影机立场与人物距离
- 叙事视角与信息控制
- 表演尺度与对白处理
- 空间、构图与景别重心
- 镜头运动与机位规则
- 光线、色彩与材质
- 剪辑与时间处理
- 声音、环境声与配乐
- 视觉母题与意象发展
- 段落变量、破例条件与禁用手法

需要查找方法案例时，先读取 [00-调用规则.md](references/style-library/00-调用规则.md)，再按需检索 `references/style-library/` 中一个相关的 A-F 文件。不要为了覆盖 A-F 而全部读取或全部引用。

输出全片视听宪法后必须人工确认；这是全流程的主要创作闸门。

### 第三步：导演节奏规划

读取并执行 [03-导演节奏规划.md](references/workflow/03-导演节奏规划.md)。输入是通过的剧本分析和全片视听宪法，不再重新选择导演风格。

逐场预规划：

- 情节节奏与情感节奏两条轨道
- 镜头密度、景别重心和相对篇幅权重
- 镜头组的叙事单元与连接方式候选（供 N5 基于最终剧本重新推导）
- 场景之间的转场策略
- 全片节奏波浪和节奏诊断

N3 不在单镜内容生成前分配场景、镜头组或单镜秒数；目标片长只登记为后续校验约束。节奏变化必须说明它如何服务导演命题。保留呼吸场景；克制不等于匀速。普通场景自动通过，硬时长约束冲突或关键段落进入抽样/人工审核。

### 第四步：剧本视听化微调

读取并执行 [04-剧本视听化微调.md](references/workflow/04-剧本视听化微调.md)。输入是原剧本、通过的视听宪法和节奏规划。

先做不可拍内容的视听化翻译，再分别检查表演节奏、台词、画面细节和意象落地。低风险执行层修改自动通过；涉及台词信息、戏剧动作、人物弧光或主要意象时暂停人工审核。O5 必须包含完整可拍执行稿，逐字保留未获确认修改的对白、旁白和画外音；“原文 → 微调 → 理由”只是修改记录，不能代替完整正文。

### 第五步：分镜拆解

N5 硬条件：每个镜头组序列必须先冻结 visual_timeline（进入、过程变化、反应、退出状态）和 visual_coverage（需要转译到九列画面栏的阶段），再估时和格式化。缺少任一字段、计时算式或计时证据都必须回到 N5.3。

N5 内容保真条件：先以原始剧本和已确认微调合成完整执行稿，为行动、对白、旁白、画外音、广播、字幕和关键声音建立逐条 `content_id` 清单。所有 ID 必须分配到镜头组和具体镜头；语言内容必须逐字进入 `spoken_segments` 与九列声音栏，不能用“对白”“同事OS”“广播”等类别摘要替代。

先读取并执行 [05-分镜拆解流程.md](references/workflow/05-分镜拆解流程.md)。该主控文件按子节点决定何时读取 [05A-镜头组与溯源规则.md](references/workflow/05A-镜头组与溯源规则.md)、[05B-镜头组内镜头序列规则.md](references/workflow/05B-镜头组内镜头序列规则.md)、[05C-九列分镜数据规范.md](references/workflow/05C-九列分镜数据规范.md) 和 [05D-分镜讨论与优化流程.md](references/workflow/05D-分镜讨论与优化流程.md)，不得一开始全部加载。05B 文件内部区分强制规则和可选设计参考，先执行强制部分，只有存在多种合理组织方式时才使用可选部分。用户在 O6/O7 生成后讨论或优化分镜时，进入 N5R；输入是通过的微调剧本、剧本分析、节奏规划、视听宪法和项目约束。

先把场景拆成戏剧节拍，再从连续节拍的共同叙事目的与关系推导镜头组。步骤 3 的镜头组只是节奏预规划，不是步骤 5 必须照抄的结果。输出含剧本来源的 `05A-镜头组拆解表.md` 并通过内部闸门 G5A 后，按镜头组队列逐组生成完整镜头序列。每组必须先冻结画面、声音和切点，再按 05B 的 `shot-timing-v2` 规则逐镜估时；不得先给组预算再把秒数分摊到镜头。已有检查点若含 `duration_budget_seconds`、缺少计时算式或没有计时证据，必须标记为 `stale` 并回到 N5.3，不能只修改批次表时间码。宿主可运行脚本时，序列写入后必须执行 `scripts/validate_timing_schema.py`；校验失败不得进入 N5.4。最后转译成九列分镜：镜号、时长、摄影角度、景别、画面内容、场景、声音、备注、叙事目的。单个镜头不是工作流循环单元；按短片、中片、长内容的规模分批，普通批次自动继续，关键或异常批次才暂停审核。

N5 还必须满足：每个 `group_id` 单独写入 `_work/N5/sequences/[组ID].md`，包含该组全部镜头和完整视觉时间线；`ALL-groups.md`、`current.md`、`sequence_note`、批次草案等聚合摘要不得作为 N5.4 或 O7 的数据源。只有所有组独立序列均通过 G5 并写入 `accepted_group_ids` 后，才能汇总 O7/O8 或标记工作流完成。

镜头选择必须先服从视听宪法，再服从本场节奏和动作逻辑。普通镜头组和批次通过检查后自动继续；开场、结尾、高潮、转折、视角变化和异常批次进入人工审核，其余按 00C 抽样。每批完成后执行三层 Double Check：导演一致性、节奏匹配、出彩镜头是否集中在叙事重心。

### 第六步：分镜表文件输出

只有完整 Markdown 分镜表完成 G5 校验后，才执行 [06-分镜文件输出.md](references/workflow/06-分镜文件输出.md)，将 `05B-分镜表.md` 确定性转换为 `.xlsx`。N6 不得重新设计或润色镜头内容。文件生成需要当前环境具备表格写入能力；不具备时输出 CSV 并保留 Markdown，不要声称已经生成 xlsx。

## 输入处理

- 开始前读取并执行 [00B-输入边界与交接协议.md](references/workflow/00B-输入边界与交接协议.md) 和 [00D-输入归档与版本协议.md](references/workflow/00D-输入归档与版本协议.md)，先归档原始剧本和项目约束，再登记 `input_mode`、项目格式、目标时长和输入版本。
- 完整剧本：直接进入第一步。
- 粗略故事或梗概：先按场景或事件单元整理，但不得擅自补写关键剧情；没有用户确认时最多推进到导演命题候选。
- 单场戏：明确分析结论只适用于该场戏，不能伪装成全片宪法或进入全片分镜。
- 编剧交接包：接收其稳定场景 ID、角色 ID、连续性状态和未决问题；`provisional` 内容必须继续标记为假设。
- 已有产物或检查点：先执行 00A 的计时 schema 迁移检查，再从最早有效节点恢复，不重新初始化游标；旧时长数字本身不构成有效检查点。
- 已有导演设想：作为候选假设，用剧本证据检验，不直接接受为结论。
- 信息不足：先完成能确定的分析，只询问会实质改变导演命题的问题。
- 已有 O6/O7 后讨论或优化分镜：进入 N5R，先定位稳定 ID、分级修改范围并输出候选方案；未确认前不改稳定产物。

## 工作纪律

- 画面判断只写摄影机可见内容；声音判断只写可听内容。
- 不用“高级感”“电影感”“氛围感”等空词代替执行规则。
- 不以导演姓名充当结论。写清具体手段、使用范围和叙事理由。
- 不因题材包含多个类型，就为每个类型分配一种导演风格。
- 不把“固定机位与手持并用”“写实与风格化结合”视为完整方案；必须说明边界和切换条件。
- 内部完成每阶段检查清单。用户要求“自检”时，再展示问题、证据和修改建议。

## 交付边界

六个阶段是一个有检查点的导演执行流程，不是一次性生成器。任何阶段未通过，都不得把草案当成已确认下游输入；若用户明确接受 `provisional` 假设，可以带风险进入下游。用户要求无依据跳步时，说明会失去哪些检查并等待明确确认；按 00B 从有效产物恢复不属于跳步。

若单个节点的问题无法在当前信息下解决，使用“暂定假设 + 风险登记 + 可回退版本”继续推进，不得因一个局部问题丢失整条工作流。

## 输出契约

本 skill 共有八类用户可见产物。N5R 的讨论记录是内部修订检查点，确认后更新 O6/O7 版本，不新增第九类正式产物。内部 `_work/` 检查点和 `00-state.yaml` 不计入产物数量。每个闸门通过后，若宿主提供文件写入能力，则将对应稳定版本写入项目运行目录；没有文件能力时，使用紧凑对话检查点。任何运行文件都不得写入 skill 安装目录。

| 编号 | 产物 | 生成节点 | 默认去向 |
|---|---|---|---|
| O1 | 剧本导演分析 | N1 | `01-剧本导演分析.md` |
| O2 | 导演命题候选方向 | N2 前半 | `02A-导演命题候选.md` |
| O3 | 全片视听宪法 | N2 后半 | `02B-全片视听宪法.md` |
| O4 | 导演节奏规划 | N3 | `03-导演节奏规划.md` |
| O5 | 剧本视听化微调稿 | N4 | `04-剧本视听化微调.md` |
| O6 | 镜头组拆解与溯源表 | N5 前半 | `05A-镜头组拆解表.md` |
| O7 | 完整 Markdown 九列分镜表 | N5 后半 | `05B-分镜表.md` |
| O8 | 最终分镜文件 | N6 | `06-[项目名称]分镜表.xlsx` |

运行目录默认为用户指定目录；未指定时，在宿主允许时使用当前项目的 `outputs/[项目名称]-director/`。按 `00A` 写入 `_work/` 和 `00-state.yaml`，记录当前位置、文件索引、通过版本、未决问题、假设与恢复入口。若环境不能生成或验证 xlsx，则输出 `06-[项目名称]分镜表.csv`，并保留 O7；必须明确标注降级原因。自检结果、风险登记和暂定假设只有在用户要求“自检”或需要解释降级时才单独展示。
